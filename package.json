{
  "name": "tiny-render",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:cov": "vitest --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.0.5",
    "typescript": "^5.2.2",
    "vite": "^5.3.5",
    "vitest": "^2.0.5"
  }
}
